# auth0-reset-password-after-signup
Boilerplate code to trigger a Reset Password flow right after a user signs up in Auth0.
Make sure that you import a library to make the post request (this example uses node-fetch@2.7.0). 
